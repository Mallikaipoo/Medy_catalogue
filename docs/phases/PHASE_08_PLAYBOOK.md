# Phase 8 playbook — Leaderboard

## Goal

Global, country, state, friends; weekly and monthly. Display name + privacy. Snapshot worker.

## Database

`leaderboard_snapshots`, `friendships` (if not already)

Never expose email/phone. Respect `leaderboard_visibility`. Client cannot POST scores.
