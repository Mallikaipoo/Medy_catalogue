# Admin console

Next.js App Router admin is implemented in **Phase 11**. This folder reserves the monorepo path.

The console must call `/api/v1/admin` only. Roles are enforced on the API, not by hiding buttons.
